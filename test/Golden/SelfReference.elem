-- Minimal program that can't be optimally evaluated without any book-keeping.

foreign export "main" main : IO (∀ 0 → 0)

foreign primitive pureIO : ∀ 0 → IO 0

main = pureIO @(∀ 0 → 0) (id @(∀ 0 → 0) id)

-- Both t and f cause problems here.
id = Λ λ0 t @0 0 0
-- id = Λ λ0 f @0 0 0

t = Λ λ0 λ0 1
-- f = Λ λ0 λ0 0

